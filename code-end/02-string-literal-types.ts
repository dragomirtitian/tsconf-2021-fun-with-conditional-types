
function withAlignment(alignment: "center" | "left" | "right" | null) {
    if (alignment) {
        alignment
    }
    
}

withAlignment("central")
withAlignment("center")
withAlignment("left")
withAlignment("right")
withAlignment("start")
withAlignment("end")
withAlignment(null)

let a: "center" = "center"

a = "aa"